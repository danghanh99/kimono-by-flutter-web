name: kimono_rental_flutter_web
description: "A new Flutter project."
publish_to: "none" # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=3.2.3 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  go_router:
  sidebarx:
  cupertino_icons:
  carousel_slider:
  font_awesome_flutter:
  dropdown_button2:

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: 

flutter:
  uses-material-design: true
  assets:
    - assets/
